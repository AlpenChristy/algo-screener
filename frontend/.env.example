# Frontend Environment Variables
# Copy this file to .env.local (for local dev) or set in Cloudflare Pages dashboard.
# Never commit .env.local to git!

# URL of your Render.com backend (leave empty for local dev — Vite proxy handles it)
# Example: https://algo-screener-backend.onrender.com
VITE_API_BASE_URL=
